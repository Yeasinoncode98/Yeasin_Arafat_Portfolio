// This is the correct, valid JavaScript content for your script.js file:

AOS.init({
  // Optional settings here
  duration: 1000,
  once: true,
});

// ... your other JavaScript code (like the typewriter logic) should also be here ...
document.addEventListener("DOMContentLoaded", initTypeWriter);
